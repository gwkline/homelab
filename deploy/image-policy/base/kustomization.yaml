apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Applied after the policy-controller helm install (its CRDs must exist
# first) and before any workload apply — see README.md for ordering.
resources:
  - clusterimagepolicy.yaml
