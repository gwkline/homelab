apiVersion: batch/v1
kind: CronJob
metadata:
  name: factory-reclaimer
  namespace: sandbox
  labels:
    app.kubernetes.io/part-of: homelab
    factory.gwkline.io/component: reclaimer
spec:
  schedule: "25 * * * *"
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 3
  suspend: false
  jobTemplate:
    spec:
      ttlSecondsAfterFinished: 86400
      activeDeadlineSeconds: 900
      backoffLimit: 0
      template:
        metadata:
          labels:
            factory.gwkline.io/profile: reclaimer
        spec:
          restartPolicy: Never
          serviceAccountName: factory-reclaimer
          automountServiceAccountToken: false
          securityContext:
            runAsNonRoot: true
            runAsUser: 1000
            seccompProfile: { type: RuntimeDefault }
          containers:
            - name: reclaimer
              image: ghcr.io/gwkline/homelab/factory/orchestrator@sha256:62603e3424bd72f57e19e39a4b05cefb2cab706a94d49c61c04aaa4c275480a4
              imagePullPolicy: Always
              command: ["sh", "/orch/run-reclaimer.sh"]
              env:
                - name: FACTORY_REPOS
                  value: gwkline/homelab,gwkline/launchpad
                - name: GH_CONFIG_DIR
                  value: /tmp/gh
                - name: FACTORY_RECLAIM_DRY_RUN
                  value: "false"
                - name: GH_TOKEN
                  valueFrom:
                    secretKeyRef:
                      { name: github-token, key: token, optional: false }
              resources:
                requests: { cpu: 100m, memory: 128Mi }
                limits: { cpu: 500m, memory: 512Mi }
              securityContext:
                allowPrivilegeEscalation: false
                capabilities: { drop: ["ALL"] }
                readOnlyRootFilesystem: true
              volumeMounts:
                - { name: tmp, mountPath: /tmp }
          volumes:
            - { name: tmp, emptyDir: {} }
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: factory-reclaimer
  namespace: sandbox
---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: factory-reclaimer
  namespace: sandbox
rules: []
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: factory-reclaimer
  namespace: sandbox
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: factory-reclaimer
subjects:
  - { kind: ServiceAccount, name: factory-reclaimer, namespace: sandbox }
