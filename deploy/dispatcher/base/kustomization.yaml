# LEGACY / DEMOTED (#78): superseded by deploy/factory/base's durable GitHub
# issue collector (apps/factory/collector) — GitHub issues are the primary
# work generator, not shell commands embedded in CronJob configuration. This
# base is kept only as the #30 behavioral smoke baseline
# (scripts/dispatch-flow-smoke.sh); do not apply it for new automation.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

labels:
  - pairs:
      app.kubernetes.io/part-of: homelab
      app.kubernetes.io/managed-by: kustomize
    includeSelectors: false

resources:
  - rbac.yaml
  - cronjob.yaml
  - netpol.yaml
