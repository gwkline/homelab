apiVersion: v1
kind: ConfigMap
metadata:
  name: t3code-workspaces
  namespace: agents
data:
  # Git URLs cloned into every replica of this StatefulSet. One URL per line.
  # Add/remove lines to change what agents can see. Private repos need the
  # github-token secret (see README -> Private repos).
  repos: |
    https://github.com/gwkline/homelab.git
