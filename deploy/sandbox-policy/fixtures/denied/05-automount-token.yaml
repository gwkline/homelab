# Fixture (denied): token automount on the default ServiceAccount — an
# unscoped API credential for whoever can read the pod.
# expect-deny: token
apiVersion: batch/v1
kind: Job
metadata:
  name: policy-deny-automount
  namespace: sandbox
  labels:
    app: policy-fixture
    policy.gwkline.io/fixture: "true"
spec:
  backoffLimit: 0
  template:
    metadata:
      labels:
        app: policy-fixture
    spec:
      serviceAccountName: default
      automountServiceAccountToken: true
      restartPolicy: Never
      containers:
        - name: evil
          image: ghcr.io/gwkline/homelab/loop-agent@sha256:e941bae94d9a59ea1c3034c3529ba633b3074ef0be5480b580a415c0e1fdfa70
          resources:
            limits:
              memory: 256Mi
