# Fixture (denied): a ServiceAccount outside the allowlist — identities are
# the escalation currency; only the repo's declared workload identities may
# be selected.
# expect-deny: allowlist
apiVersion: batch/v1
kind: Job
metadata:
  name: policy-deny-foreign-sa
  namespace: sandbox
  labels:
    app: policy-fixture
    policy.gwkline.io/fixture: "true"
spec:
  backoffLimit: 0
  template:
    metadata:
      labels:
        app: policy-fixture
    spec:
      serviceAccountName: node-admin
      automountServiceAccountToken: true
      restartPolicy: Never
      containers:
        - name: evil
          image: ghcr.io/gwkline/homelab/loop-agent@sha256:e941bae94d9a59ea1c3034c3529ba633b3074ef0be5480b580a415c0e1fdfa70
          resources:
            limits:
              memory: 256Mi
