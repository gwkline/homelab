# Fixture (denied): a privileged container that is NOT the approved dind
# shape — privileged is reserved for the exact dind sidecar.
# expect-deny: privileged
apiVersion: batch/v1
kind: Job
metadata:
  name: policy-deny-privileged
  namespace: sandbox
  labels:
    app: policy-fixture
    policy.gwkline.io/fixture: "true"
spec:
  backoffLimit: 0
  template:
    metadata:
      labels:
        app: policy-fixture
    spec:
      automountServiceAccountToken: false
      restartPolicy: Never
      containers:
        - name: malware
          image: ghcr.io/gwkline/homelab/loop-agent@sha256:e941bae94d9a59ea1c3034c3529ba633b3074ef0be5480b580a415c0e1fdfa70
          securityContext:
            privileged: true
          resources:
            limits:
              memory: 256Mi
